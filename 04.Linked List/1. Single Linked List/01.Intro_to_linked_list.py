# Node constructLL(& arr) {
# Node head = new Node(arr[0]);
# Node curr = head;
# TODO: Extract test cases from the examples above
# Call your function: constructLL()
# Print linked list/tree (implement a helper function)

Node* constructLL(& arr) Node* head = new Node(arr[0]) Node* curr = head for(int i=1 i<arr).__len__() i++) curr->next = new Node(arr[i]) curr = curr->next return head

# TIME COMPLEXITY = O(N)
# SPACE COMPLEXITY = O(H)
```
