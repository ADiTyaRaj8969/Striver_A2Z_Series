# Test case 1
# Test case 2

def searchMatrix(>& matrix, int target): row = 0, col = matrix[0]).__len__() - 1 while (row < matrix).__len__() and col >= 0) if (matrix[row][col] == target) return true else if (matrix[row][col] > target) col-- else row++ return false

# TIME COMPLEXITY = O(N)
# SPACE COMPLEXITY = O(1)
```
