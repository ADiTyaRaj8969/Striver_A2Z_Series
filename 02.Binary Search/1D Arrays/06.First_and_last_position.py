# if the target is not found, return [-1, -1]
# Test case 1
# TIME COMPLEXITY: O(log n)

def searchRange(& nums, int target): first = lower_bound(nums.begin(), nums.end(), target) - nums.begin()
# SPACE COMPLEXITY = O(1)
```
